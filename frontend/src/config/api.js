// API configuration for the student buddy frontend

// In a production environment, you would use environment variables
// But for now, we'll use a hardcoded URL to avoid the 'process is not defined' error
const API_BASE_URL = 'http://localhost:8001/api';

export { API_BASE_URL };
