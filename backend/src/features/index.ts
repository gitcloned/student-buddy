// Import all feature implementations here to ensure they are registered
import './ChapterTeachingFeature';

// Export the Feature class and FeatureRegistry for external use
export { Feature } from './Feature';
export { FeatureRegistry } from './FeatureRegistry';
